Що вже реалізовано:
* парсер предикатів { умова } [ дії ] → таблиці MAIN і CONNECT
* стековий алгоритм розбору виразів з парами лексем і трасуванням
* граф НСА
* генерація C++ коду з перевіркою Mark
* React + TypeScript + Vite архітектура з чіткими сервісами
* проміжні стани (normal) — зараз всі стани крім першого є final
* мітки і goto для переходів між станами

Що обговорювалось але не реалізовано:
* вкладені предикати (відклали)


На першому етапі визначити всі дані та їх типи.
Подумати про паралельні обчислення бо ядро лізе в памʼять і потрібно то робити через монітор
Схоже на макроси в асемблері. Потрібно зробити щось подібне.
Стековий алгоритм це головна кілерфіча. Він не тільки виконує арифметичні операції а й розпізнає патерни в моделі та конвертує їх в згенерований код. Одним словом все зав'язано на стековому алгоритмі.